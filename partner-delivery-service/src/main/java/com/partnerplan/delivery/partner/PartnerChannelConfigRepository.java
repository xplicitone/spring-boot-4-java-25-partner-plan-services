package com.partnerplan.delivery.partner;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PartnerChannelConfigRepository extends JpaRepository<PartnerChannelConfig, String> {
}
